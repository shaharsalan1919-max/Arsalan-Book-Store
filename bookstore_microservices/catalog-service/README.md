# Catalog Service (Go)

Simple HTTP API to manage a book catalog persisted in `catalog.json` (for demo simplicity).

Endpoints:
- GET /books
- GET /books/{id}
- POST /books
- PUT /books/{id}
- DELETE /books/{id}
